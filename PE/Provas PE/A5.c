#include <stdio.h>
#include <stdlib.h>

//funcao que realiza a conta desejada
int valida(int n);
long double constroi(int n,int q,int m);

int main(void) {
  long double*v;
  int n,q,m,i;

  scanf("%d %d %d",&n,&q,&m);
  v=malloc(sizeof(int)*m);


  printf("%lf",);
  free(v);
  return 0;
}
